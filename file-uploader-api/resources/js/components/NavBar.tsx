import { FC } from 'react';

import UploadFile from './UploadFile';

const Navbar: FC = () => {
  return (
    <div className="navbar">
      <UploadFile />
    </div>
  );
};

export default Navbar;
